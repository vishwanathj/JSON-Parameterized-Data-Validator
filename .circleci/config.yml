# Golang CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-go/ for more details
version: 2
jobs:
  gosec_lint:
    machine:
      enabled: true
    working_directory: ~/go/src/github.com/vishwanathj/JSON-Parameterized-Data-Validator
    steps:
      - checkout
      - run: make docker-gosec
      - run: make docker-lint
  tests:
    docker:
      - image: circleci/golang:1.11.2

      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/postgres:9.4

    #### TEMPLATE_NOTE: go expects specific checkout path representing url
    #### expecting it in the form of
    ####   /go/src/github.com/circleci/go-tool
    ####   /go/src/bitbucket.org/circleci/go-tool
    working_directory: /go/src/github.com/vishwanathj/JSON-Parameterized-Data-Validator
    steps:
      - checkout
      #- setup_remote_docker

      # specify any bash command here prefixed with `run: `
      #- run: go get -v -t -d ./...
      - run: go test -v ./... -count=1 -tags=unit
      - run: make unit
      - store_artifacts:
          path: /go/src/github.com/vishwanathj/JSON-Parameterized-Data-Validator/test_results
workflows:
  version: 2
  workflow:
    jobs:
      - gosec_lint
      - tests:
          requires:
            - gosec_lint